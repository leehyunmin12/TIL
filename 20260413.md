# prototype

Date: 2026년 4월 13일
Review Needed: Yes

생성자

singleton, flyweight, prototype, builder

분리

templete method, strategy, bridge

동일시

decorator, composite, command

상태

state, memento, observer

연결

iterator, chain of responsibility, facade

복습

singleton : 객체를 하나만 만들고 새로 만들지 않고 만들었던 객체를 계속 사용

flyweight : 객체들을 변하는 것(extrinsic), 변하지 않는 것(intrinsic)을 분류하여 각각 따로 클래스를 만들어서 메모리를 최대한 절약한다!

## prototype!

: 기존 객체의 복사를 통해 우리가 필요한 것을 추가하여 새로운 객체를 생성하는 디자인 패턴 중 하나

→ 이 때 복사된 객체를 프로토타입 객체(Prototype)이라고 한다.

java의 clonable 인터페이스 상속,  clone() 메서드 오버라이딩.

주의할 점 : 생성하고자 하는 객체의 클래스에서 clone()이 정의되어 있어야한다.

장점 : 

- 객체 생성 비용과 시간 절약
- 새로운 객체 생성 시 오버헤드 감소
- 객체 생성 방법이 복잡하거나 객체의 타입이 동적으로 결정되는 경우 유용

간단히 말해서 DB의 데이터를 가져오는 객체 같이 비용이 많이 드는 행위를 하는 객체를 매번 new 하지 않고 clone() 하여 사용하면 비용과 메모리 절약이 가능하다는 것이다.

예시 코드

여기서 clone()이라는 메서드를 오버라이딩하여 현재 로딩한 DB를 똑같이 복사하여 return해준다.

```java
public class Employees implements Cloneable{
 
    private List<String> empList;
	
    public Employees(){
        empList = new ArrayList<String>();
    }
	
    public Employees(List<String> list){
        this.empList=list;
    }
    
    public void loadData(){
        //read all employees from database and put into the list
        empList.add("Pankaj");
        empList.add("Raj");
        empList.add("David");
        empList.add("Lisa");
    }
	
    public List<String> getEmpList() {
        return empList;
    }
 
    @Override
    public Object clone() throws CloneNotSupportedException{
        List<String> temp = new ArrayList<String>();
        for(String s : this.empList){
            temp.add(s);
        }
        return new Employees(temp);
    }
	
}
```

그럼 Main에서는 .clone() 메서드로 복사하여 사용 가능!

```java
public class PrototypePatternTest {
 
    public static void main(String[] args) throws CloneNotSupportedException {
        Employees emps = new Employees();
        emps.loadData();
		
        //Use the clone method to get the Employee object
        Employees empsNew = (Employees) emps.clone();
        Employees empsNew1 = (Employees) emps.clone();
        List<String> list = empsNew.getEmpList();
        list.add("John");
        List<String> list1 = empsNew1.getEmpList();
        list1.remove("Pankaj");
		
        System.out.println("emps List: "+emps.getEmpList());
        System.out.println("empsNew List: "+list);
        System.out.println("empsNew1 List: "+list1);
    }
 
}
```