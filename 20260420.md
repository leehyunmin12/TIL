# [DB] ElasticSearch 1차

Date: 2026년 4월 20일
Review Needed: No

Elasticsearch란?

검색 기능을 매우 강력하고 유연하게 제공하는 **분산형 검색 및 분석 엔진**

특징

- 분산형 : 여러 대의 서버에 데이터를 자동 분산 저장하고 병렬로 처리
- RESTful API 기반 : HTTP 요청만으로 손쉽게 데이터 등록/검색/집계 가능
- 검색 + 분석 : 단순 검색 뿐 아니라, 대용량 로그 분석, 통계, 집계, 비정형 데이터 처리에 특화
- 실시간 처리 : 거의 실시간으로 데이터 색인 및 검색 가능

Apache Lucene 기반

Lucene의 복잡함을 감싸고,

- REST API 제공
- 클러스터/노드/샤드 구조로 분산처리
- 스케일아웃(서버 추가)가 가능함.

JSON 기반 문서(Document) 저장

RDB처럼 row를 저장하는 구조가 아닌, JSON 포맷의 문서(document) 단위로 데이터를 저장함.

```java
{
  "user": "홍길동",
  "message": "Elasticsearch 스터디 중입니다",
  "timestamp": "2025-03-25T09:00:00"
}
```

Elasticsearch 동작 

1. 사용자가 JSON 데이터를 Elasticsearch에 저장 요청
2. Elasticsearch는 내부적으로 Lucene 인덱스를 생성
3. 검색 요청 시 Lucene이 색인을 빠르게 탐색
4. 결과를 Elasticsearch가 HTTP 응답으로 반환

![image.png](image.png)

클러스터 : 여러 개의 노드가 네트워크로 연결 된 것, 노드의 집합이다.

노드 : Elasticsearch의 기본 작동 단위이다.

인덱스 : 도큐먼트를 모아놓은 집합, RDB로 치면 테이블

샤드 : 각 샤드는 독립적인 ‘인덱스’라고 할 수 있음. 자체적으로 검색 가능